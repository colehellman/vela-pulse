FROM postgres:16
# pg_partman is in the PostgreSQL apt extension repo for Debian (postgres:16 is Debian-based).
RUN apt-get update \
    && apt-get install -y --no-install-recommends postgresql-16-partman \
    && rm -rf /var/lib/apt/lists/*
